server:
  port: 7777
#  port: 8080
spring:
  profiles:
    active: dev
  main:
    allow-circular-references: true
  datasource:
    druid:
      driver-class-name: ${sky.datasource.driver-class-name}
      url: jdbc:mysql://${sky.datasource.host}:${sky.datasource.port}/${sky.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${sky.datasource.username}
      password: ${sky.datasource.password}
  redis:
    host: localhost
    port: 6379
    database: 1

mybatis:
  #mapper配置文件
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.sky.entity
  configuration:
    #开启驼峰命名
    map-underscore-to-camel-case: true

logging:
  level:
    com:
      sky:
        mapper: debug
        service: info
        controller: info

sky:
  jwt:
    # 设置jwt签名加密时使用的秘钥
    admin-secret-key: codeCreater
    # 设置jwt过期时间
    admin-ttl: 72000000
    # 设置前端传递过来的令牌名称
    admin-token-name: token
    # 用户端token设置
    user-secret-key: gameplayer
    user-ttl: 72000000
    user-token-name: authentication
  alioss:
    endpoint: oss-ap-southeast-1.aliyuncs.com
    access-key-id: LTAI5tHH3Z3rgw9cre1Ladrt
    access-key-secret: WEIbwdURK29HbI0Fw2fhibCFGWlkff
    bucket-name: codecreater
  wechat:
    appid: wx7d681d79cbe2919d
    secret: 124f6870d568b361aaff929dc8fd0a5d
#    mchid:
#    mch-serial-no:
#    private-key-file-path:
#    api-v3-key:
#    we-chat-pay-cert-file-path:
#    notify-url:
#    refund-notify-url:

  shop:
    address: 成都市成华区二仙桥东三路1号
  baidu:
    ak: hekHSG9cXoSLHxx4HODKAGeeiVlGXLWG
#    sk:
